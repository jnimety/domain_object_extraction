# Add files and commands to this file, like the example:
#   watch('file/path') { `command(s)` }
#
guard 'shell' do
  watch( /(.*).textile/ ) {|m| `slideshow -t jnimety-g5.txt #{m[0]}` }
  #watch( /(.*).textile/ ) {|m| `slideshow -t g5.txt #{m[0]}` }
  #watch( /(.*).textile/ ) {|m| `slideshow -t s6syntax.txt #{m[0]}` }
end

# vim: filetype=ruby
